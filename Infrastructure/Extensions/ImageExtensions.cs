namespace Infrastructure.Extensions;

public static class ImageExtensions
{
    //public static Image? ResizeImage(Image? srcImage, int width)
    //{
    //    if (srcImage != null && RuntimeInformation.IsOSPlatform(OSPlatform.Windows))
    //    {
    //        var bitmap = new Bitmap(width, srcImage.Height * width / srcImage.Width);
    //        using (var g = Graphics.FromImage(bitmap))
    //        {
    //            g.InterpolationMode = InterpolationMode.HighQualityBicubic;
    //            g.DrawImage(srcImage, 0, 0, bitmap.Width, bitmap.Height);
    //        }
    //        return bitmap;
    //    }
    //    return srcImage;
    //}
}
