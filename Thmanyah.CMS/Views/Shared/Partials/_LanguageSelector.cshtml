@* @{
    Layout = null;
    var currentLanguage = _localeService.GetByCode(_sessionProvider.GetCurrentLocale());
}
<div class="menu-item px-5" data-kt-menu-trigger="{default: 'click', lg: 'hover'}"
     data-kt-menu-placement="left-start" data-kt-menu-offset="-15px, 0">
    <a href="#" class="menu-link px-5">
        <span class="menu-title position-relative">
           @Resources.Language
            <span class="fs-8 rounded bg-light px-3 py-2 position-absolute translate-middle-y top-50 end-0">
                @currentLanguage.Title <img class="w-15px h-15px rounded-1 ms-2" src="/assets/media/locales/@(currentLanguage.Code).svg" alt="language flag icon">
            </span>
        </span>
    </a>
    <div class="menu-sub menu-sub-dropdown w-175px py-4">

        @foreach (var item in _localeService.GetAllActiveLanguages())
        {
            <div class="menu-item px-3">
                <a asp-area="" asp-controller="languages" asp-action="change" asp-route-locale="@item.Code" class="menu-link d-flex px-5 @(currentLanguage.Code == item.Code ? "active" : string.Empty)">
                    <span class="symbol symbol-20px me-4">
                        <img class="rounded-1" src="/assets/media/locales/@(item.Code).svg" alt="language flag icon">
                    </span>
                    @item.Title
                </a>
            </div>
        }
    </div>
</div>
 *@